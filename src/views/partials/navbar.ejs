<nav class="navbar navbar-expand-lg glass-nav fixed-top py-3">
    <div class="container">
        <!-- Brand -->
        <a class="navbar-brand d-flex align-items-center gap-2" href="/">
            <img src="/images/logo.png" alt="Logo" style="height: 40px;">
            <div class="d-none d-sm-block">
                <span class="d-block fw-black lh-1" style="font-size: 1.2rem; color: var(--color-primary); letter-spacing: -0.5px;">BÌNH LỢI</span>
                <span class="d-block" style="font-size: 0.6rem; color: var(--color-text-muted); letter-spacing: 2px; font-weight: 700;">HEALING JOURNEY</span>
            </div>
        </a>

        <!-- Toggle -->
        <button class="navbar-toggler border-0 shadow-none p-0" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
            <i class="bi bi-list fs-1 text-primary"></i>
        </button>

        <!-- Nav Content -->
        <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar" style="width: 320px;">
            <div class="offcanvas-header p-4">
                <div class="d-flex align-items-center gap-2">
                    <img src="/images/logo.png" alt="Logo" style="height: 32px;">
                    <span class="fw-black text-primary">BÌNH LỢI</span>
                </div>
                <button type="button" class="btn-close shadow-none" data-bs-dismiss="offcanvas"></button>
            </div>
            <div class="offcanvas-body">
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0 gap-1">
                    <li class="nav-item">
                        <a class="nav-link-premium" href="/">Trang chủ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link-premium" href="/explore">Khám phá</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link-premium" href="/workshops">Workshop</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link-premium" href="/reviews">Cộng đồng</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link-premium" href="/market">Chợ OCOP</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link-premium" href="/map">Bản đồ</a>
                    </li>
                </ul>
                
                <!-- Auth Section -->
                <div class="d-flex flex-column flex-lg-row align-items-lg-center gap-3">
                    <% if (user) { %>
                        <div class="dropdown">
                            <a href="#" class="d-flex align-items-center gap-2 text-decoration-none glass-card p-1 pe-3" data-bs-toggle="dropdown">
                                <img src="<%= user.avatar || '/images/default-avatar.png' %>" class="rounded-circle" style="width: 36px; height: 36px; object-fit: cover;">
                                <span class="d-none d-lg-inline fw-bold small"><%= user.full_name.split(' ').pop() %></span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end glass-card border-0 p-2 mt-3" style="min-width: 220px;">
                                <li><a class="dropdown-item py-2 rounded-3" href="/profile"><i class="bi bi-person me-2"></i>Hồ sơ</a></li>
                                <li><a class="dropdown-item py-2 rounded-3" href="/passport"><i class="bi bi-qr-code me-2"></i>Passport</a></li>
                                <% if (user.role === 'admin') { %>
                                    <li><a class="dropdown-item py-2 rounded-3 fw-bold text-primary" href="/admin"><i class="bi bi-shield-check me-2"></i>Quản trị</a></li>
                                <% } %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item py-2 rounded-3 text-danger" href="/auth/logout"><i class="bi bi-box-arrow-right me-2"></i>Đăng xuất</a></li>
                            </ul>
                        </div>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-cinematic px-4 py-2 small" style="font-size: 0.8rem;">Đăng nhập</a>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</nav>

<style>
    .nav-link-premium {
        color: var(--color-text);
        font-weight: 600;
        text-decoration: none;
        padding: 10px 20px;
        border-radius: 50px;
        transition: var(--transition-smooth);
        font-size: 0.9rem;
    }
    
    .nav-link-premium:hover {
        background: var(--color-primary);
        color: white;
    }
    
    @media (max-width: 991.98px) {
        .navbar-nav { padding-top: 20px; }
        .nav-link-premium { display: block; border-radius: 12px; }
    }
</style>

<style>
    /* Hamburger */
    .nav-hamburger { width: 24px; height: 18px; position: relative; cursor: pointer; }
    .nav-hamburger span { display: block; height: 2px; background: var(--primary); border-radius: 2px; transition: all 0.3s ease; position: absolute; left: 0; width: 100%; }
    .nav-hamburger span:nth-child(1) { top: 0; }
    .nav-hamburger span:nth-child(2) { top: 8px; width: 70%; }
    .nav-hamburger span:nth-child(3) { top: 16px; width: 85%; }

    /* Dropdown polish */
    .dropdown-item { font-size: 0.85rem; transition: all 0.15s ease; }
    .dropdown-item:hover { background: var(--primary-50); color: var(--primary); }
    .dropdown-item:active { background: var(--primary-100); }

    @media (min-width: 992px) {
        #offcanvasNavbar {
            position: static !important;
            transform: none !important;
            visibility: visible !important;
            width: auto !important;
            flex-grow: 1;
            background: transparent !important;
            backdrop-filter: none !important;
            border: none !important;
        }
        .offcanvas-header { display: none; }
        .offcanvas-body { display: flex; padding: 0; overflow-y: visible; }
        .border-lg-0 { border: none !important; }
    }
    
    @media (max-width: 991.98px) {
        #offcanvasNavbar {
            background: rgba(255,255,255,0.98) !important;
            backdrop-filter: blur(20px) !important;
        }
        .navbar-nav .nav-link {
            padding: 14px 16px !important;
            font-size: 0.95rem !important;
            border-radius: var(--radius-md);
        }
        .navbar-nav .nav-link:hover { background: var(--primary-50); }
    }
</style>