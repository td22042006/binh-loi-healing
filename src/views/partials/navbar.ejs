<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom sticky-top py-3">
  <div class="container">
    
    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center" href="/">
      <img src="/images/logo.png" alt="Logo" style="height: 45px;">
      <span class="ms-2 d-none d-sm-inline fw-bold text-primary" style="font-size: 1.1rem; text-transform: uppercase; letter-spacing: 1px;">Bình Lợi Healing</span>
    </a>

    <!-- Toggle Button for Mobile -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menu Links -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-lg-4 me-auto mb-2 mb-lg-0 gap-1">
        <li class="nav-item">
          <a class="nav-link fw-bold text-uppercase px-3 <%= typeof title !== 'undefined' && title.includes('Trang chủ') ? 'active text-primary' : '' %>" href="/">Trang chủ</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold text-uppercase px-3 <%= typeof title !== 'undefined' && title.includes('Địa điểm') ? 'active text-primary' : '' %>" href="/destinations">Địa điểm</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold text-uppercase px-3 <%= typeof title !== 'undefined' && title.includes('Hành trình') ? 'active text-primary' : '' %>" href="/onboarding">Hành trình</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold text-uppercase px-3" href="/map">Bản đồ</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold text-uppercase px-3" href="/checkin">Check-in</a>
        </li>
      </ul>

      <!-- Right Side: Auth / Profile -->
      <div class="d-flex align-items-center gap-3">
        <% if (user) { %>
          <div class="dropdown">
            <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle text-dark" data-bs-toggle="dropdown">
              <img src="<%= user.avatar || '/images/default-avatar.png' %>" class="rounded-circle border border-2 border-primary p-1" style="width: 40px; height: 40px; object-fit: cover;">
              <span class="ms-2 d-none d-md-inline fw-bold"><%= user.full_name %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2 rounded-3">
              <li><a class="dropdown-item py-2" href="/summary"><i class="bi bi-award me-2"></i>Thành tích</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item py-2 text-danger" href="/auth/logout"><i class="bi bi-box-arrow-right me-2"></i>Đăng xuất</a></li>
            </ul>
          </div>
        <% } else { %>
          <a href="/auth/login" class="btn btn-outline-primary rounded-pill px-4 fw-bold">Đăng nhập</a>
          <a href="/auth/register" class="btn btn-primary rounded-pill px-4 fw-bold">Đăng ký</a>
        <% } %>
      </div>
    </div>

  </div>
</nav>

<style>
  .navbar-nav .nav-link { font-size: 0.8rem; letter-spacing: 0.5px; transition: all 0.3s ease; }
  .navbar-nav .nav-link:hover { color: var(--v5-primary) !important; }
  .navbar-nav .nav-link.active { color: var(--v5-primary) !important; position: relative; }
  @media (min-width: 992px) {
    .navbar-nav .nav-link.active::after {
      content: ''; position: absolute; bottom: 0; left: 20px; right: 20px; height: 3px; background: var(--v5-primary); border-radius: 10px;
    }
  }
</style>