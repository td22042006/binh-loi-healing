<nav class="navbar navbar-expand-lg navbar-premium fixed-top">
    <div class="container">
        <!-- Brand -->
        <a class="navbar-brand d-flex align-items-center gap-2" href="/">
            <img src="/images/logo.png" alt="Logo" style="height: 36px;">
            <div>
                <span class="d-block fw-black lh-1" style="font-size: 1.1rem; color: var(--primary);">BÌNH LỢI</span>
                <span class="d-block" style="font-size: 0.6rem; color: var(--text-muted); letter-spacing: 2px; font-weight: 600;">HEALING JOURNEY</span>
            </div>
        </a>

        <!-- Toggle -->
        <button class="navbar-toggler border-0 shadow-none p-0" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
            <div class="nav-hamburger">
                <span></span><span></span><span></span>
            </div>
        </button>

        <!-- Nav Content -->
        <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar" style="width: 320px;">
            <div class="offcanvas-header p-4">
                <div class="d-flex align-items-center gap-2">
                    <img src="/images/logo.png" alt="Logo" style="height: 28px;">
                    <span class="fw-black" style="color: var(--primary);">BÌNH LỢI</span>
                </div>
                <button type="button" class="btn-close shadow-none" data-bs-dismiss="offcanvas"></button>
            </div>
            <div class="offcanvas-body">
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0 gap-1">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="bi bi-house-door d-lg-none me-2"></i>Trang chủ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/explore"><i class="bi bi-compass d-lg-none me-2"></i>Khám phá</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/workshops"><i class="bi bi-palette d-lg-none me-2"></i>Workshop</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/reviews"><i class="bi bi-chat-heart d-lg-none me-2"></i>Cộng đồng</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/journey"><i class="bi bi-signpost-split d-lg-none me-2"></i>Hành trình</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/map"><i class="bi bi-map d-lg-none me-2"></i>Bản đồ</a>
                    </li>
                </ul>
                
                <!-- Auth Section -->
                <div class="d-flex flex-column flex-lg-row align-items-lg-center gap-2 mt-4 mt-lg-0 pt-4 pt-lg-0 border-top border-lg-0">
                    <% if (user) { %>
                        <!-- Cart removed -->
                        <!-- User Dropdown -->
                        <div class="dropdown">
                            <a href="#" class="d-flex align-items-center gap-2 text-decoration-none p-1 rounded-pill" data-bs-toggle="dropdown" style="background: var(--primary-50); padding-right: 14px !important;">
                                <img src="<%= user.avatar || '/images/default-avatar.png' %>" class="rounded-circle" style="width: 34px; height: 34px; object-fit: cover; border: 2px solid var(--primary-100);">
                                <span class="d-none d-lg-inline fw-bold" style="font-size: 0.82rem; color: var(--primary);"><%= user.full_name.split(' ').pop() %></span>
                                <i class="bi bi-chevron-down d-none d-lg-inline" style="font-size: 0.65rem; color: var(--text-muted);"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end border-0 shadow-lg p-2 mt-2" style="border-radius: var(--radius-lg); min-width: 220px;">
                                <li class="px-3 py-2 mb-1">
                                    <div class="fw-bold" style="font-size: 0.88rem;"><%= user.full_name %></div>
                                    <div style="font-size: 0.75rem; color: var(--text-muted);"><%= user.email %></div>
                                </li>
                                <li><hr class="dropdown-divider my-1"></li>
                                <li><a class="dropdown-item py-2 rounded-2" href="/profile"><i class="bi bi-person me-2 text-muted"></i>Hồ sơ du khách</a></li>
                                <li><a class="dropdown-item py-2 rounded-2" href="/my-workshops"><i class="bi bi-ticket-perforated me-2 text-muted"></i>Workshop của tôi</a></li>
                                <li><a class="dropdown-item py-2 rounded-2" href="/profile/rewards"><i class="bi bi-gift me-2 text-muted"></i>Điểm thưởng</a></li>
                                <li><a class="dropdown-item py-2 rounded-2" href="/passport"><i class="bi bi-qr-code me-2 text-muted"></i>Passport số</a></li>
                                <% if (user.role === 'manager') { %>
                                    <li><hr class="dropdown-divider my-1"></li>
                                    <li><a class="dropdown-item py-2 rounded-2" href="/manager"><i class="bi bi-speedometer2 me-2 text-muted"></i>Quản lý điểm đến</a></li>
                                <% } %>
                                <% if (user.role === 'admin') { %>
                                    <li><a class="dropdown-item py-2 rounded-2" href="/admin" style="color: var(--primary); font-weight: 600;"><i class="bi bi-shield-check me-2"></i>Admin Panel</a></li>
                                <% } %>
                                <li><hr class="dropdown-divider my-1"></li>
                                <li><a class="dropdown-item py-2 rounded-2 text-danger" href="/auth/logout"><i class="bi bi-box-arrow-right me-2"></i>Đăng xuất</a></li>
                            </ul>
                        </div>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-premium rounded-pill px-4 py-2 fw-bold" style="font-size: 0.82rem;">Đăng nhập</a>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</nav>

<style>
    /* Hamburger */
    .nav-hamburger { width: 24px; height: 18px; position: relative; cursor: pointer; }
    .nav-hamburger span { display: block; height: 2px; background: var(--primary); border-radius: 2px; transition: all 0.3s ease; position: absolute; left: 0; width: 100%; }
    .nav-hamburger span:nth-child(1) { top: 0; }
    .nav-hamburger span:nth-child(2) { top: 8px; width: 70%; }
    .nav-hamburger span:nth-child(3) { top: 16px; width: 85%; }

    /* Dropdown polish */
    .dropdown-item { font-size: 0.85rem; transition: all 0.15s ease; }
    .dropdown-item:hover { background: var(--primary-50); color: var(--primary); }
    .dropdown-item:active { background: var(--primary-100); }

    @media (min-width: 992px) {
        #offcanvasNavbar {
            position: static !important;
            transform: none !important;
            visibility: visible !important;
            width: auto !important;
            flex-grow: 1;
            background: transparent !important;
            backdrop-filter: none !important;
            border: none !important;
        }
        .offcanvas-header { display: none; }
        .offcanvas-body { display: flex; padding: 0; overflow-y: visible; }
        .border-lg-0 { border: none !important; }
    }
    
    @media (max-width: 991.98px) {
        #offcanvasNavbar {
            background: rgba(255,255,255,0.98) !important;
            backdrop-filter: blur(20px) !important;
        }
        .navbar-nav .nav-link {
            padding: 14px 16px !important;
            font-size: 0.95rem !important;
            border-radius: var(--radius-md);
        }
        .navbar-nav .nav-link:hover { background: var(--primary-50); }
    }
</style>